<p id="notice"><%= notice %></p>

<% if user_signed_in? && current_user.role == "candidate" %>
  <%= render 'candidate_signed_in_page', :candidate => @candidate %>
<% else %>

<h6>Candidatos</h6>

<nav class="navbar navbar-expand-lg navbar-light" style="margin-bottom: 25px">

<svg style="position: absolute; padding-left:5px" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
  <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
</svg>
  <form class="form-inline">
    <input class="form-control mr-sm-2" type="search" placeholder="  search" aria-label="Search", style="font-size: small">
  </form>

    <div class="dropdown" style="padding-left: 50px">

      <button class="btn btn-secondary btn-light dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" 
        aria-haspopup="true" aria-expanded="false", style="font-size: small">
        Área Profissional
      </button>

      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
        <a class="dropdown-item" href="#">Área 1</a>
      </div>

    </div>

    <div class="dropdown" style="padding-left: 50px">

      <button class="btn btn-secondary btn-light dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" 
        aria-haspopup="true" aria-expanded="false", style="font-size: small">
        Situação Profissional
      </button>

      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
        <a class="dropdown-item" href="#">Situação 1</a>
      </div>

    </div>

    <div class="dropdown" style="padding-left: 50px">

    <button class="btn btn-secondary btn-light dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" 
      aria-haspopup="true" aria-expanded="false">
      Concelho
    </button>

    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
      <a class="dropdown-item" href="#">Concelho 1</a>
    </div>

  </div>

</nav>

<%# divise helper if logged in and if admin = CRUD is visible %>

<h1>Candidates CRUD visible to admin</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Industry</th>
      <th>Employed</th>
      <th>Grade</th>
      <th>Qualification</th>
      <th>Experience</th>
      <th>Address</th>
      <th>Location</th>
      <th>Phone</th>
      <th>Fax</th>
      <th>Email</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @candidates.each do |candidate| %>
      <tr>
        <td><%= candidate.name %></td>
        <td><%= candidate.description %></td>
        <td><%= candidate.industry %></td>
        <td><%= candidate.employed %></td>
        <td><%= candidate.grade %></td>
        <td><%= candidate.qualification %></td>
        <td><%= candidate.experience %></td>
        <td><%= candidate.address %></td>
        <td><%= candidate.location %></td>
        <td><%= candidate.phone %></td>
        <td><%= candidate.fax %></td>
        <td><%= link_to 'Show', candidate %></td>
        <td><%= link_to 'Edit', edit_candidate_path(candidate) %></td>
        <td><%= link_to 'Destroy', candidate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Candidate', new_candidate_path %>

<% end %>
