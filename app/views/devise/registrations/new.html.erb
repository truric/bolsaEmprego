<% if params[:role] == "entity" || @user.role == "entity" %>

  <%= render 'form_new_entity', entity: @entity %>
    
<% else %>

  <%= render 'form_new_candidate', candidate: @candidate %>

<% end %>